package slice

// Equal compares two string slices if they contain the same elements in the same order.
func Equal(x, y []string) bool {
	if len(x) != len(y) {
		return false
	}
	for i := range x {
		if x[i] != y[i] {
			return false
		}
	}
	return true
}
